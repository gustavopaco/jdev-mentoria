package com.pacoprojects.dto;

import com.pacoprojects.model.CupomDesconto;

import java.io.Serializable;

/**
 * A DTO for the {@link CupomDesconto} entity
 */
public record CupomDescontoDtoBasicId(Long id) implements Serializable {
}
